# LogiqAI runs on real AI only (Google Gemini). A key is REQUIRED.
# Get a free key at https://aistudio.google.com  ->  "Get API key".
# Without a valid key, /api/analyze returns 503 "temporarily unavailable".
GEMINI_API_KEY=

# Optional overrides
LOGIQAI_MODEL=gemini-2.5-flash
