// ============================================================================
// ШАБЛОН: единственное место, где нужно вписать данные своего бизнеса.
// Всё, что здесь экспортируется, сразу отражается в приложении — без правки
// остального кода. Подробности и скриншоты — см. README.md, раздел
// "Настройка под свой бизнес".
// ============================================================================

// Название студии/салона — заголовок в карточке "О нас" (страница "О нас" и
// блок в "Настройках").
export const BUSINESS_NAME = 'GlowSpace'

// Подзаголовок под приветствием на главной странице, сразу после BUSINESS_NAME.
export const BUSINESS_TAGLINE = 'пространство твоего сияния'

// Абзац с описанием в карточке "О нас", под названием.
export const ABOUT_DESCRIPTION =
  'GlowSpace — уютная студия красоты в центре города. Косметология, маникюр, ' +
  'брови и ресницы, массаж лица. Работаем только на профессиональной косметике, ' +
  'соблюдаем стерильность и подбираем уход индивидуально под вашу кожу.'

// Заголовок и текст блока в самом низу раздела "О нас" (например, про
// сотрудников/мастеров). Пустой SPECIALISTS_TEXT — блок не показывается.
export const SPECIALISTS_TITLE = 'Наша команда'
export const SPECIALISTS_TEXT =
  'Мастера GlowSpace — сертифицированные специалисты с опытом от 5 лет. ' +
  'Регулярно проходим обучение и следим за трендами индустрии, чтобы вы уходили ' +
  'от нас в отличном настроении и с идеальным результатом.'

// Контакты — построчно в разделе "О нас". Пустая строка у любого поля — эта
// строка просто не показывается (остальные показываются как обычно).
export const CONTACTS = {
  address: 'ул. Тверская, 18, Москва',
  phone: '+7 (999) 214-08-56',
  hours: 'Ежедневно: 10:00–21:00',
  instagram: '@glowspace_studio',
}

// Баннер на главной странице, под 4 карточками меню. Пустой imageUrl —
// баннер просто не показывается. Картинку разместить в webapp/public/
// (тогда imageUrl = '/имя-файла.jpg') либо указать любой внешний URL.
// Рекомендуемое соотношение сторон — 2:1 (например 800×400) — баннер
// показывается в этом соотношении с обрезкой (object-cover), под ширину
// экрана, чтобы не зависеть от исходного размера файла.
export const HOME_BANNER = {
  imageUrl: '',
  // Необязательно: куда вести при тапе на баннер (акция, внешняя ссылка и т.п.)
  linkUrl: '',
}

// Ссылка на личные сообщения с мастером в Telegram, например
// 'https://t.me/username'. Пустая строка — кнопка "Связаться с мастером"
// в Настройках не показывается.
export const MASTER_CONTACT_URL = 'https://t.me/glowspace_studio'
